import { Wrapper } from './Wrapper';
export declare class FixLeftWrapper extends Wrapper {
    private length;
    constructor(length: number, uint8Array: Uint8Array);
}
export declare class FixRightWrapper extends Wrapper {
    private length;
    constructor(length: number, uint8Array: Uint8Array);
}
